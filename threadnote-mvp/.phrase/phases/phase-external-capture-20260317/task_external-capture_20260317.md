task001 [x] 场景:用户从应用外快速记录文字或链接 | Given:Threadnote 已启动且存在 workspace | When:用户按下 Cmd+Shift+V 并在 quick capture 浮层中输入内容后发送 | Then:系统创建一条 inbox entry + 浮层关闭 + 不要求用户先选 thread | 验证:全局快捷键手动测试+clean-room application service tests
task002 [x] 场景:用户显式导入剪贴板图片或文本 | Given:quick capture 浮层已打开且剪贴板中有文本或图片 | When:用户点击 Import Clipboard | Then:文本进入正文 + 图片进入附件 + attachments-only 也可提交 | 验证:手动测试+clean-room attachment tests
task003 [x] 场景:用户从 Finder 拖文件到 quick capture | Given:quick capture 浮层已打开且 workspace 可写 | When:用户拖入一个或多个文件并发送 | Then:文件复制到 attachments 目录 + 同一条 entry 保存全部附件 | 验证:手动测试+clean-room attachment manager tests
task004 [x] 场景:用户取消 quick capture 后稍后继续编辑 | Given:quick capture 浮层已有未发送草稿 | When:用户关闭或取消窗口后再次打开 quick capture | Then:窗口隐藏而不是销毁 + 当前进程内草稿仍保留 | 验证:手动测试
task005 [x] 场景:应用退出时不被 quick capture 阻塞 | Given:quick capture 窗口存在且已启用 close-to-hide | When:用户执行 Quit | Then:窗口允许真正销毁 + 应用正常退出 | 验证:手动测试+代码检查
task006 [x] 场景:用户使用 Cmd+Enter 或 Ctrl+Enter 发送 capture | Given:主界面或 quick capture 输入框已有内容且未处于 IME 组合态/补全确认态 | When:用户按下发送快捷键或在异步提交完成前重复触发 | Then:编辑器稳定发送一次 + 提交中禁用输入/按钮 + 不生成重复 entry | 验证:clean-room editor runtime tests+主界面/quick capture 手动测试
task007 [x] 场景:用户看到 AI 正在处理具体工作 entry | Given:某条 entry 正在做 AI 路由判断或其所属 thread 正在做 AI 整理 | When:用户浏览 Stream 或 Thread 中的该条 entry | Then:entry 前方显示淡入淡出圆点与简短状态文案 + 处理完成后自动消失 | 验证:clean-room application/presenter tests+renderer entry card tests+手动测试
task008 [x] 场景:用户在 Stream 提交内容后立即看到结果而不是等待 AI 完成 | Given:Stream 输入框已有内容且 AI 路由/线程整理可能较慢 | When:用户提交一条 capture | Then:输入框在 IPC 返回后立即恢复可用 + 新 entry 立刻出现在 Stream + AI 后续路由与线程刷新通过后台事件异步更新 | 验证:clean-room application tests+renderer useWorkbench tests+主界面手动测试
task009 [x] 场景:AI 后台结果回写后普通文本 entry 仍稳定渲染 | Given:Stream 或 Thread 中已显示一条普通文本 entry 且后台会异步回写 aiActivity/路由结果 | When:renderer 收到 workbench-updated 并刷新该 entry | Then:界面不触发 Rendered more hooks than during the previous render + ErrorBoundary 可打印完整组件栈用于定位 + entry 卡片在 AI 状态/线程状态切换后继续正常显示 | 验证:renderer threadNavigation tests+renderer useWorkbench tests+主界面手动测试
task010 [x] 场景:AI 后台编排在失败或切换 provider 后不再残留 processing 状态 | Given:route/resume/prepare/discourse 任务可能排队、被更新覆盖、provider 切换或因上下文/网络失败中断 | When:后台 AI 任务被 supersede、abort、失败或 provider 被重配 | Then:旧任务会真正取消并释放 queue 槽位 + workbench/thread 刷新仍会广播最新快照 + `AI 正在判断归档位置`/`AI 正在整理线程` 自动消失 | 验证:clean-room application tests+clean-room threadnoteAIService tests+主界面手动测试
task011 [x] 场景:用户在 Electron clean-room 中看到与 Swift 同级的 AI 后台状态与自动收敛 | Given:route/resume/prepare/discourse 已具备基础任务能力但缺少统一状态机、失效 sweep 与 inspector/debug 可观测性 | When:entry 新增/改动、thread 打开、provider 切换或 AI 返回 invalid planner/失败结果 | Then:application 层统一暴露 route debug、thread AI status、prepared view + 后台 sweep 自动补 route/resume/discourse + Stream/Thread inspector 可见 processing/failed/invalidPlan/notConfigured 状态且 provider 切换后旧内存态立即清空 | 验证:clean-room application tests+renderer threadNavigation tests+主界面手动测试
